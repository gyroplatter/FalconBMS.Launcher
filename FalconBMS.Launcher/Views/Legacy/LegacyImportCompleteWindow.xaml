<Window x:Class="FalconBMS.Launcher.Views.LegacyImportCompleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to the New Launcher"
        Width="640"
        Height="300"
        MinWidth="640"
        MinHeight="300"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppWindowBackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTextBlock"
                   Grid.Row="0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Text="Welcome to the new BMS Launcher"/>

        <TextBlock x:Name="IntroTextBlock"
                   Grid.Row="1"
                   Margin="0,14,0,0"
                   TextWrapping="Wrap"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Text="Your old Launcher control setup was found and imported into this new Launcher."/>

        <TextBlock x:Name="BackupTextBlock"
                   Grid.Row="2"
                   Margin="0,12,0,0"
                   TextWrapping="Wrap"
                   Style="{StaticResource BodyTextBlockStyle}">
            <Run x:Name="BackupIntroRun"
                 Text="Before importing your old controls, a backup of your setup was created in your BMS installation folder:"/>
            <LineBreak/>
            <Run x:Name="BackupFolderRun"
                 FontWeight="SemiBold"
                 Text="User\Config\Launcher-Import-Backup"/>
        </TextBlock>

        <StackPanel x:Name="ImportMessagesPanel"
                    Grid.Row="3"
                    Margin="0,20,0,0">

            <TextBlock x:Name="ImportMessagesHeaderTextBlock"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Style="{StaticResource DialogSectionLabelStyle}"
                       Text="Import Messages"/>

            <TextBox x:Name="ImportMessagesTextBox"
                     Height="120"
                     Padding="10"
                     IsReadOnly="True"
                     IsReadOnlyCaretVisible="False"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Text="Controls that could not be imported"/>
        </StackPanel>

        <Button x:Name="ContinueButton"
                Grid.Row="4"
                Width="120"
                Height="34"
                Margin="0,22,0,0"
                HorizontalAlignment="Right"
                IsDefault="True"
                Style="{StaticResource TertiaryButtonStyle}"
                Content="Continue"
                Click="ContinueButton_Click"/>
    </Grid>
</Window>