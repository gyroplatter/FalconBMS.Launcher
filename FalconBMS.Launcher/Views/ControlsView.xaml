<UserControl x:Class="FalconBMS.Launcher.Views.ControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:FalconBMS.Launcher.Controls"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded">

    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedAxisRows"
                              Source="{Binding AxisRows}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="GroupName"/>
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Controls"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1"
                Padding="12"
                BorderThickness="1"
                BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
      Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="90"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
               Text="Axis"
               FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2"
               Text="Binding"
               FontWeight="SemiBold"/>
                </Grid>

                <ListView Grid.Row="1"
                          ItemsSource="{Binding Source={StaticResource GroupedAxisRows}}"
                          BorderThickness="0"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          HorizontalContentAlignment="Stretch">
                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border Margin="0,10,0,6"
                                            Padding="0,6,0,4"
                                            BorderThickness="0,0,0,1"
                                            BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch"/>
                            <Setter Property="Padding"
                                    Value="0"/>
                            <Setter Property="Margin"
                                    Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="300"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding DisplayName}"
                                           VerticalAlignment="Center"/>

                                <controls:AxisBar Grid.Column="1"
                                                  ShowLabels="False"
                                                  BarWidth="200"
                                                  Value="{Binding AxisBarValue}"
                                                  IsActive="{Binding AxisBarEnabled}"
                                                  FillBrush="{Binding AxisBarFillBrush}"
                                                  OverlayText="{Binding AxisBarOverlayText}"
                                                  OverlayVisibility="{Binding AxisBarOverlayVisibility}"
                                                  ShowDetentMarkers="{Binding ShowDetentMarkers}"
                                                  IdleDetentFraction="{Binding IdleDetentFraction}"
                                                  AbDetentFraction="{Binding AbDetentFraction}" />

                                <TextBlock Grid.Column="2"
                                           Text="{Binding BindingText}"
                                           VerticalAlignment="Center"
                                           TextWrapping="Wrap"/>

                                <Button Grid.Column="3"
                                        Content="Assign"
                                        Command="{Binding AssignCommand}"
                                        Margin="6,0,0,0"/>

                                <Button Grid.Column="4"
                                        Content="Clear"
                                        Command="{Binding ClearCommand}"
                                        Margin="6,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2"
                   Margin="0,12,0,0"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   Text="{Binding StatusText}"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>