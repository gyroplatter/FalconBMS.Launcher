<Window x:Class="FalconBMS.Launcher.Views.LegacyImportCompleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Complete"
        Width="580"
        Height="430"
        MinWidth="580"
        MinHeight="430"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppWindowBackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Text="Import Complete"/>

        <TextBlock Grid.Row="1"
                   Margin="0,10,0,10"
                   TextWrapping="Wrap"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Text="Your existing Launcher controls have been imported into the new Launcher."/>

        <TextBlock Grid.Row="2"
                   Margin="0,0,0,18"
                   TextWrapping="Wrap"
                   Style="{StaticResource BodyTextBlockStyle}">
                    A backup of your old Launcher control files has been created in your BMS install folder:
                    <LineBreak/>
                    \Config\User\Launcher-Import-Backup
                  </TextBlock>

        <StackPanel x:Name="SkippedControlsPanel"
                    Grid.Row="3"
                    Visibility="Collapsed">

            <TextBlock FontSize="16"
                       FontWeight="SemiBold"
                       Style="{StaticResource DialogSectionLabelStyle}"
                       Text="Some controls could not be imported"/>

            <TextBlock Margin="0,0,0,10"
                       TextWrapping="Wrap"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Text="Please remap the controls below manually."/>

            <TextBox x:Name="SkippedControlsTextBox"
                     Height="150"
                     Padding="10"
                     IsReadOnly="True"
                     IsReadOnlyCaretVisible="False"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"/>
        </StackPanel>

        <Button Grid.Row="4"
                Width="110"
                Height="34"
                Margin="0,22,0,0"
                HorizontalAlignment="Right"
                IsDefault="True"
                Style="{StaticResource TertiaryButtonStyle}"
                Content="Continue"
                Click="ContinueButton_Click"/>
    </Grid>
</Window>